<nav class="navbar navbar-default">
    <div class="container-fluid">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <% if current_user.is_normal_super_admin? %>
                <%= link_to "MSturnos", super_admin_path, :class => "navbar-brand" %>
            <% end %>
            <% if current_user.is_normal_admin? %>
                <%= link_to "MSturnos", admin_path, :class => "navbar-brand" %>
            <% end %>
            <% if current_user.is_normal_user? %>
                <%= link_to "MSturnos", portal_path, :class => "navbar-brand" %>
            <% end %>            
        </div>
        <div id="navbar" class="navbar-collapse collapse">
        <% if user_signed_in? %> 
        <ul class="nav navbar-nav">
            <% if current_user.is_normal_super_admin? %>
                <li><a href="#">Administradores</a></li>
                <li><%= link_to "Empresas", companies_path %></li>   
                <li><%= link_to "Rubros", areas_path %></li>
            <% end %>
            <% if current_user.is_normal_admin? %>
                <li><%= link_to "Sucursales", branch_company_path(company_id: current_user.companies.first.id, branch_id: 0) %></li>
                <li><%= link_to "Datos de la empresa", current_user.companies.first %></li>
            <% end %>
            <% if current_user.is_normal_user? %>
                <li><a href="#">Buscar empresa</a></li>
            <% end %>            
        </ul>
        <% end %>
        <ul class="nav navbar-nav navbar-right">
            <% if user_signed_in? %>        
                <li><%= link_to "Cerrar sesion", destroy_user_session_path, method: :delete %></li>
            <% else %>
                <li><%= link_to "Iniciar sesion", new_user_session_path %></li>
            <% end %> 
        </ul>
        </div><!--/.nav-collapse -->
    </div><!--/.container-fluid -->
</nav>