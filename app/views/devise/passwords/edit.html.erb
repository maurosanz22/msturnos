<%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :put }) do |f| %>
  <%= devise_error_messages! %>
  <%= f.hidden_field :reset_password_token %>
<div class="container">

  <div class="col-md-4"></div>
      <div class="col-md-4"> 
      <form class="form-signin">
          
        <h3>Cambie su contraseña?</h3>
        
          <div class="field">
           </br><label>Nueva contraseña</label><em> <%= @minimum_password_length %> caracteres minimos</em>
            <%= f.password_field :password, autofocus: true, autocomplete: "off",class: 'form-control', type:"password" %></br>
          </div>

          <div class="field">
            <label>Confirmar contraseña</label>
            <%= f.password_field :password_confirmation, autocomplete: "off", class: 'form-control', type:"password" %></br>
          </div>
          <div class="actions">
            <%= f.submit "Cambiar Contraseña", class: "btn btn-lg btn-primary btn-block" %>
          </div>
      </form>
      <%= render "devise/shared/links" %>
    </div>
  <div class="col-md-4"></div>
</div>
<% end %>

<style>
  body {
    background-color: #eee;
  }
</style>
